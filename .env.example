# Local secrets — never commit real keys.
# Copy to .env and fill in as needed.

# Path to twcs.csv or sample.csv
DATA_PATH=

# Optional overrides
SELECTED_BRAND=
SAMPLE_SIZE=
RANDOM_SEED=42

# Secrets (leave empty until needed)
KAGGLE_USERNAME=
KAGGLE_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
LLM_MODEL=
EMBEDDING_MODEL=
